1.Process.Exit() -> The process.exit() method is used to end the process which is running at the same time with an exit code in NodeJS
    Code: It can be either 0 or 1. 0 means end the process without any kind of failure and 1 means end the process with some failure.

2.app.use(express.json()) -> The express.json() function is a built-in middleware function in Express. It parses incoming requests with JSON payloads and is based on body-parser. 
3.Push -> update new 
4.Pull -> Delete
5.{new:true} -> return updated entry not old entry
6.Populate("comments") -> we have a id's of a comment but we can fetch actual document associated with it
7.The create() method is used specifically to create and insert a new document into the MongoDB collection.
8.The save() method is used to update an existing document in the database or create a new document if it doesn't exist.